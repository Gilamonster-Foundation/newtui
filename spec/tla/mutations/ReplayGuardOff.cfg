\* EXPECT-RED: CoversColdReach
\*
\* BUG 3b, LIVE AT 7f69a3d. The only genuine live catch in the whole exercise.
\*
\* "drain": the FIRST `factory()` call returns the warm machine and every later
\* one returns the cold machine — newt-tui's `static BUFFER: OnceLock<Mutex<..>>`
\* whose `take()` at type_ahead.rs:66 drains, consumed inside component
\* construction. `explore.rs:225-228` replays the path into a fresh component
\* and nothing checks that it landed where discovery said it would.
\*
\* The counterexample IS the bug. The identical configuration with
\* `GuardOn = TRUE` is ../DepartureGuardCloses.cfg and it is green.

SPECIFICATION Spec

CONSTANTS
    DepthCaps  = {3}
    WorldModes = {"drain"}
    GuardOn    = FALSE
    Narrow     = TRUE
    WarmStart  = "w0"

INVARIANT CoversColdReach
