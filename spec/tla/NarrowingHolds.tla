---- MODULE NarrowingHolds ----
\* A CONFIGURATION of ExplorerReplay, not a copy of it. The module body lives
\* in ExplorerReplay.tla; this shim exists only because check.sh discovers
\* <Name>.tla + <Name>.cfg pairs and TLC binds CONSTANTS per module.
EXTENDS ExplorerReplay
====
